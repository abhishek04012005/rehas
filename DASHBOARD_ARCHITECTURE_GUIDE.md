# Admin Dashboard Architecture & Visual Guide

## System Architecture Diagram

```
┌─────────────────────────────────────────────────────────────────┐
│                        REHAS Admin Panel                         │
│                    (http://localhost:3000)                       │
└─────────────────────────────────────────────────────────────────┘
                               │
                ┌──────────────┼──────────────┐
                │              │              │
        ┌───────▼─────┐  ┌────▼────┐  ┌─────▼────────┐
        │   Dashboard │  │ Contacts │  │  Enquiries   │
        │ (existing)  │  │ (exist)  │  │  (existing)  │
        └─────────────┘  └──────────┘  └──────────────┘
                │              │              │
                ├──────────────┼──────────────┤
                │              │              │
        ┌───────▼──────────────▼────────────▼─────────┐
        │          Admin Navigation Bar (UPDATED)     │
        │  Logo | Dashboard | Contacts | Enquiries   │
        │        Orders (NEW) | Payments (NEW)        │
        └───────┬──────────────┬────────────┬─────────┘
                │              │            │
        ┌───────▼─────────┐ ┌─▼───────┐ ┌─▼────────────────┐
        │ Orders Dashboard│ │Payments │ │ (more features)  │
        │  [NEW]          │ │Dashboard│ │                  │
        │                 │ │ [NEW]   │ │                  │
        └─────────────────┘ └─────────┘ └──────────────────┘
                │                 │
                └────────┬────────┘
                         │
                    ┌────▼─────────────────┐
                    │  Supabase Database   │
                    │   orders table       │
                    └─────────────────────┘
```

---

## Orders Dashboard Flow

```
┌──────────────────────────────────┐
│  /admin/orders Page              │
│  OrdersDashboard Component       │
└─────────────────┬────────────────┘
                  │
                  ├─→ Authentication Check (localStorage)
                  │
                  ├─→ Fetch Orders from Supabase
                  │   SELECT * FROM orders
                  │
                  ├─→ Calculate Statistics:
                  │   - Total Orders
                  │   - Pending Count
                  │   - Confirmed Count
                  │   - Completed Count
                  │   - Total Revenue
                  │   - Paid Revenue
                  │
                  ├─→ Render Layout:
                  │   ┌─────────────────────────────┐
                  │   │ Header & Statistics (6 Cards)│
                  │   ├─────────────────────────────┤
                  │   │ Search & Filters            │
                  │   │ - Search Box                │
                  │   │ - Status Filter             │
                  │   │ - Payment Filter            │
                  │   ├─────────────────────────────┤
                  │   │ Orders Table                │
                  │   │ - ID, Name, Product         │
                  │   │ - Amount, Statuses, Date    │
                  │   │ - View Buttons              │
                  │   └─────────────────────────────┘
                  │
                  └─→ Modal System:
                      Click View → Opens Modal
                      ├─ Display Order Details
                      ├─ Update Order Status
                      │  ├─ Mark as Confirmed
                      │  ├─ Mark as Completed & Paid
                      │  └─ Cancel Order
                      └─ UPDATE orders SET...
```

---

## Payments Dashboard Flow

```
┌──────────────────────────────────┐
│  /admin/payments Page            │
│  PaymentsDashboard Component     │
└─────────────────┬────────────────┘
                  │
                  ├─→ Authentication Check (localStorage)
                  │
                  ├─→ Fetch Orders from Supabase
                  │   SELECT * FROM orders
                  │
                  ├─→ Calculate Advanced Statistics:
                  │   - Total Transactions
                  │   - Completed Transactions
                  │   - Pending Transactions
                  │   - Failed Transactions
                  │   - Total Amount (₹)
                  │   - Completed Amount (₹)
                  │   - Average Amount (₹)
                  │   - Success Rate (%)
                  │
                  ├─→ Render Layout:
                  │   ┌──────────────────────────────┐
                  │   │ Header & Statistics (8 Cards) │
                  │   ├──────────────────────────────┤
                  │   │ Search & Filters             │
                  │   │ - Search Box                 │
                  │   │ - Payment Status Filter      │
                  │   │ - Order Type Filter          │
                  │   │ - Export CSV Button          │
                  │   ├──────────────────────────────┤
                  │   │ Payments Table               │
                  │   │ - ID, Name, Email, Product  │
                  │   │ - Amount, Status, Date       │
                  │   │ - View Buttons               │
                  │   └──────────────────────────────┘
                  │
                  ├─→ Export System:
                  │   Click Export → Generate CSV
                  │   └─ Download: payments-YYYY-MM-DD.csv
                  │
                  └─→ Modal System:
                      Click View → Opens Modal
                      ├─ Display Payment Details
                      ├─ Update Payment Status
                      │  ├─ Mark as Paid
                      │  ├─ Mark as Failed
                      │  └─ Mark as Unpaid
                      └─ UPDATE orders SET payment_status...
```

---

## Navigation Structure

### Before (3 routes)
```
REHAS Admin Panel
├── Dashboard (/admin/dashboard)
├── Contacts (/admin/dashboard) [duplicate link]
└── Enquiries (/admin/enquiries)
```

### After (5 routes) ✅
```
REHAS Admin Panel
├── Dashboard (/admin/dashboard)
├── Contacts (/admin/dashboard)
├── Enquiries (/admin/enquiries)
├── Orders (/admin/orders) [NEW]
└── Payments (/admin/payments) [NEW]
```

---

## Data Flow Diagram

```
┌──────────────────────────────────────────────────────┐
│              Supabase Database                       │
│         orders table (all orders & payments)         │
│                                                      │
│  Columns:                                           │
│  - id, full_name, email, phone_number              │
│  - product_title, amount, currency                 │
│  - status (order_status), payment_status           │
│  - order_type, service_id, service_title           │
│  - created_at, updated_at                          │
│  - razorpay_payment_id, razorpay_order_id         │
└─────────────────┬──────────────────────────────────┘
                  │
        ┌─────────┼──────────┐
        │         │          │
   ┌────▼─┐  ┌───▼──┐  ┌────▼────┐
   │ Read │  │Update│  │  Filter │
   │SELECT│  │UPDATE│  │ WHERE   │
   └──┬──┘  └───┬──┘  └────┬─────┘
      │        │           │
      │    ┌───▼──────┬────▼──────┐
      │    │          │           │
  ┌───▼────▼──┐  ┌────▼──┐  ┌────▼────┐
  │   Orders  │  │Payment│  │Filtered │
  │ Dashboard │  │Dash   │  │  Data   │
  └───────────┘  └───────┘  └─────────┘
```

---

## Component Hierarchy

```
AdminLayout
│
├── AdminNavbar (Updated with 5 links)
│   ├── Dashboard link
│   ├── Contacts link
│   ├── Enquiries link
│   ├── Orders link (NEW)
│   ├── Payments link (NEW)
│   └── Logout button
│
├── Routes
│   ├── /admin/dashboard → ContactDashboard
│   ├── /admin/enquiries → EnquiryDashboard
│   ├── /admin/orders → OrdersDashboard (NEW)
│   │   ├── Header
│   │   ├── StatsGrid (6 cards)
│   │   ├── ControlsSection
│   │   │   ├── SearchBox
│   │   │   ├── StatusFilter
│   │   │   └── PaymentFilter
│   │   ├── TableSection
│   │   │   └── OrdersTable
│   │   └── Modal (Order Details)
│   │       ├── OrderInfo
│   │       └── ActionButtons
│   │
│   └── /admin/payments → PaymentsDashboard (NEW)
│       ├── Header
│       ├── StatsGrid (8 cards)
│       ├── ControlsSection
│       │   ├── SearchBox
│       │   ├── PaymentStatusFilter
│       │   ├── OrderTypeFilter
│       │   └── ExportButton
│       ├── TableSection
│       │   └── PaymentsTable
│       └── Modal (Payment Details)
│           ├── PaymentInfo
│           └── StatusButtons
│
└── Footer (existing)
```

---

## Status Flow Diagram - Orders

```
                    NEW ORDER
                        │
                        ▼
                  ┌──────────┐
                  │ PENDING  │ ← Initial state
                  └────┬─────┘
                       │ Admin clicks "Mark as Confirmed"
                       ▼
                  ┌──────────┐
                  │ CONFIRMED│ ← Service scheduled
                  └────┬─────┘
                       │ Service completed
                       ▼
                  ┌──────────┐
                  │ COMPLETED│ ← Service delivered
                  └──────────┘
                  
       (Any state) ─→ CANCELLED ← Admin cancels
```

---

## Status Flow Diagram - Payments

```
                 ORDER PLACED
                        │
                        ▼
                  ┌──────────┐
                  │  UNPAID  │ ← Initial state
                  └────┬─────┘
                       │ Payment received
                       ▼
                  ┌──────────┐
                  │   PAID   │ ← Payment processed
                  └──────────┘
                  
        └────────→ FAILED ←────── Payment failed/declined
                        │
                        │ Retry/Retry
                        ▼
                      UNPAID
```

---

## Statistics Calculation Logic

### Orders Dashboard

```
┌─────────────────────────────────────────────┐
│  Statistics Calculation                     │
├─────────────────────────────────────────────┤
│                                             │
│  Total Orders = COUNT(ALL)                 │
│  Pending = COUNT(status = 'pending')       │
│  Confirmed = COUNT(status = 'confirmed')   │
│  Completed = COUNT(status = 'completed')   │
│  Total Revenue = SUM(amount)                │
│  Paid Revenue = SUM(amount WHERE           │
│                    payment_status='paid')   │
│                                             │
└─────────────────────────────────────────────┘
```

### Payments Dashboard

```
┌─────────────────────────────────────────────┐
│  Statistics Calculation                     │
├─────────────────────────────────────────────┤
│                                             │
│  Total Transactions = COUNT(ALL)            │
│  Completed = COUNT(payment_status='paid')  │
│  Pending = COUNT(payment_status='unpaid')  │
│  Failed = COUNT(payment_status='failed')   │
│  Total Amount = SUM(amount)                 │
│  Completed Amount = SUM(amount WHERE       │
│                      payment_status='paid') │
│  Average Amount = Total / COUNT             │
│  Success Rate = (Completed/Total) × 100    │
│                                             │
└─────────────────────────────────────────────┘
```

---

## Responsive Breakpoints

```
┌─────────────────────────────────────────────┐
│         Desktop: 1200px+                    │
│                                             │
│  [LOGO] [Nav Items] [User] [Logout]        │
│  ┌──────────────────────────────────────┐  │
│  │ Stat Card │ Stat Card │ Stat Card   │  │
│  │ Stat Card │ Stat Card │ Stat Card   │  │
│  └──────────────────────────────────────┘  │
│  [Search ───────] [Filter] [Filter]        │
│  ┌──────────────────────────────────────┐  │
│  │ Col │ Col │ Col │ Col │ Col │ Col   │  │
│  │ ────────────────────────────────── │  │
│  │ Row │ Row │ Row │ Row │ Row │ Row   │  │
│  └──────────────────────────────────────┘  │
└─────────────────────────────────────────────┘

┌─────────────────────────────────┐
│    Tablet: 768px - 1199px       │
│                                 │
│ [LOGO] [Nav] [User] [☰]        │
│ ┌─────────────────────────────┐ │
│ │ Stat Card │ Stat Card       │ │
│ │ Stat Card │ Stat Card       │ │
│ └─────────────────────────────┘ │
│ [Search ──────────]              │
│ [Filter] [Filter]               │
│ ┌─────────────────────────────┐ │
│ │ Col │ Col │ Col │ [scroll] │ │
│ │ Row │ Row │ Row │ ────────►│ │
│ └─────────────────────────────┘ │
└─────────────────────────────────┘

┌──────────────────┐
│ Mobile: <768px   │
│                  │
│ [LOGO] [☰]      │
│ ┌──────────────┐ │
│ │ Stat Card    │ │
│ │ Stat Card    │ │
│ │ Stat Card    │ │
│ └──────────────┘ │
│ [Search]         │
│ [Filter]         │
│ [Filter]         │
│ [Export] (if)    │
│ ┌──────────────┐ │
│ │ Col | Col    │ │
│ │ Row | Row    │ │
│ │ [scroll►]    │ │
│ └──────────────┘ │
└──────────────────┘
```

---

## File Organization

```
rehas-website/
├── src/
│   ├── admin/
│   │   ├── ordersDashboard/          ← NEW
│   │   │   ├── ordersDashboard.tsx
│   │   │   └── ordersDashboard.module.css
│   │   ├── paymentsDashboard/        ← NEW
│   │   │   ├── paymentsDashboard.tsx
│   │   │   └── paymentsDashboard.module.css
│   │   ├── adminNavbar/              ← UPDATED
│   │   │   ├── adminNavbar.tsx
│   │   │   └── adminNavbar.module.css
│   │   ├── contactDashboard/
│   │   ├── enquiryDashboard/
│   │   └── login/
│   └── app/
│       └── admin/
│           ├── orders/               ← NEW
│           │   └── page.tsx
│           ├── payments/             ← NEW
│           │   └── page.tsx
│           ├── dashboard/
│           ├── enquiries/
│           └── login/
│
└── Documentation/
    ├── DASHBOARD_IMPLEMENTATION_REPORT.md
    ├── DASHBOARD_QUICK_REFERENCE.md
    └── DASHBOARD_FINAL_SUMMARY.md
```

---

## Color Coding System

```
┌──────────────────────────────────────────┐
│           Status Badges                  │
├──────────────────────────────────────────┤
│                                          │
│  Order Status:                           │
│  🔵 Pending      → Blue #3b82f6         │
│  🟣 Confirmed    → Purple #a855f7       │
│  🟢 Completed    → Green #22c55e        │
│  🔴 Cancelled    → Red #ef4444          │
│                                          │
│  Payment Status:                         │
│  🟡 Unpaid       → Amber #f59e0b        │
│  🟢 Paid         → Green #10b981        │
│  🔴 Failed       → Red #ef4444          │
│                                          │
└──────────────────────────────────────────┘
```

---

## Performance Timeline

```
Dashboard Load Sequence:

0ms   ├─ Page loaded
      │
50ms  ├─ Component mounted
      │
100ms ├─ Auth check (localStorage)
      │
150ms ├─ Supabase query initiated
      │
400ms ├─ Data received from DB
      │
450ms ├─ Statistics calculated
      │
500ms ├─ UI rendered
      │
550ms ├─ Animations complete
      │
600ms └─ Ready for interaction

Total Load Time: ~500-600ms
(Time may vary based on network)
```

---

## Integration Points

```
┌─────────────────────────────────────────┐
│         Next.js Application             │
├─────────────────────────────────────────┤
│                                         │
│  ┌─────────────────────────────────┐   │
│  │   React Components              │   │
│  │   - OrdersDashboard             │   │
│  │   - PaymentsDashboard           │   │
│  │   - AdminNavbar (updated)       │   │
│  └────────────┬────────────────────┘   │
│               │                         │
│  ┌────────────▼────────────────────┐   │
│  │   Supabase Client               │   │
│  │   - Query: SELECT * FROM orders │   │
│  │   - Update: UPDATE orders SET   │   │
│  └────────────┬────────────────────┘   │
│               │                         │
│  ┌────────────▼────────────────────┐   │
│  │   Supabase Backend              │   │
│  │   - PostgreSQL Database         │   │
│  │   - RLS Policies                │   │
│  │   - Indexes for Performance     │   │
│  └─────────────────────────────────┘   │
│                                         │
│  ┌─────────────────────────────────┐   │
│  │   localStorage                  │   │
│  │   - adminSession token          │   │
│  │   - adminUsername               │   │
│  │   - adminId                     │   │
│  └─────────────────────────────────┘   │
│                                         │
│  ┌─────────────────────────────────┐   │
│  │   Material-UI Icons             │   │
│  │   - ShoppingCart, CreditCard    │   │
│  │   - CheckCircle, Error, Search  │   │
│  └─────────────────────────────────┘   │
│                                         │
└─────────────────────────────────────────┘
```

---

**Diagram Version**: 1.0  
**Last Updated**: December 24, 2025  
**Status**: Architecture Complete ✅
